# ===========================================
# CapStack Environment Configuration
# ===========================================
# Copy this file to .env and fill in your values
# Never commit .env to version control!

# ----- App Settings -----
APP_NAME=CapStack API
APP_ENV=development
DEBUG=true

# ----- Database -----
# SQLite (default for local development)
DATABASE_URL=sqlite+aiosqlite:///./capstack.db

# PostgreSQL (uncomment for production)
# DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/capstack

# ----- LLM Configuration -----
# LiteLLM supports 100+ providers. Set the API key for your chosen provider.
# Model format: provider/model-name (e.g., "gpt-4o", "azure/gpt-4", "ollama/llama2")

LITELLM_MODEL=gpt-4o-mini

# OpenAI
OPENAI_API_KEY=

# Azure OpenAI (use model format: azure/deployment-name)
AZURE_API_KEY=
AZURE_API_BASE=https://your-resource.openai.azure.com
AZURE_API_VERSION=2024-02-15-preview

# Anthropic (use model format: claude-3-sonnet-20240229)
ANTHROPIC_API_KEY=

# Local models with Ollama (use model format: ollama/llama2)
OLLAMA_BASE_URL=http://localhost:11434

# ----- Auth -----
# Set to enable API key authentication on /api/* routes.
# Leave empty to disable (default).
API_KEY=

# ----- CORS -----
# Comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
